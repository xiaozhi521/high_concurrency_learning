    同步控制是并发程序必不可少的重要手段。synchronized 关键字就是一种简单的控制方法。他决定了一个线程是否可以访
问临界区资源。同时，Object.wait() 和 Object.notify() 方法起到了线程等待和通知的作用。这些工具对于实现复杂的多线程
协作起到了重要的作用。这个包中介绍了 synchronized 、Object.wait() 和 Object.notify() 方法的替代品（或者说增强版）
-----重入锁。